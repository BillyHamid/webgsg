/**
 * Core domain types — single source of truth for parcel states,
 * roles, and actions across the entire system.
 */
export declare const PARCEL_STATES: readonly ["registered", "received_warehouse", "preparing", "shipped", "in_transit", "arrived_country", "customs", "out_for_delivery", "delivered", "cancelled"];
export type ParcelState = (typeof PARCEL_STATES)[number];
export declare const TERMINAL_PARCEL_STATES: readonly ParcelState[];
export declare function isTerminalState(state: ParcelState): boolean;
export declare const PARCEL_STATE_LABELS: Record<ParcelState, string>;
export declare const PAYMENT_STATES: readonly ["pending", "partial", "paid", "refunded", "waived"];
export type PaymentState = (typeof PAYMENT_STATES)[number];
export declare const PAYMENT_STATE_LABELS: Record<PaymentState, string>;
export declare const PAYMENT_MODES: readonly ["cash", "mobile_money", "bank_transfer", "card"];
export type PaymentMode = (typeof PAYMENT_MODES)[number];
export declare const PAYMENT_TIMINGS: readonly ["at_shipping", "at_arrival"];
export type PaymentTiming = (typeof PAYMENT_TIMINGS)[number];
export declare const PAYMENT_TIMING_LABELS: Record<PaymentTiming, string>;
export declare const PARCEL_ACTIONS: readonly ["scan_in_warehouse", "assign_to_box", "box_departed", "in_flight", "box_arrived", "declare_customs", "customs_cleared", "deliver", "cancel"];
export type ParcelAction = (typeof PARCEL_ACTIONS)[number];
export interface ParcelTransition {
    from: ParcelState | '*';
    to: ParcelState;
    action: ParcelAction;
    /** Required metadata keys on the transition request */
    requires?: ReadonlyArray<'photo' | 'recipient_signature' | 'reason' | 'box_id' | 'geo'>;
}
/**
 * THE STATE MACHINE.
 * Wildcard `*` matches any non-terminal state.
 * Order matters: first match wins on conflict.
 */
export declare const PARCEL_TRANSITIONS: readonly ParcelTransition[];
export declare const ROLES: readonly ["super_admin", "admin_global", "agent_usa", "agent_bf", "caissier", "manutentionnaire"];
export type Role = (typeof ROLES)[number];
export declare const ROLE_LABELS: Record<Role, string>;
export declare const BOX_TYPES: readonly ["CARGO", "EXPRESS"];
export type BoxType = (typeof BOX_TYPES)[number];
export declare const BOX_STATUSES: readonly ["open", "shipped", "in_transit", "arrived", "closed"];
export type BoxStatus = (typeof BOX_STATUSES)[number];
export declare const PARCEL_CATEGORIES: readonly ["PHONE", "ELECTRONICS", "COMPUTER", "COSMETICS", "FOOD", "CLOTHING", "DOCUMENTS", "OTHER"];
export type ParcelCategory = (typeof PARCEL_CATEGORIES)[number];
export declare const PARCEL_CATEGORY_LABELS: Record<ParcelCategory, string>;
export declare const CURRENCIES: readonly ["USD", "XOF", "EUR"];
export type Currency = (typeof CURRENCIES)[number];
/**
 * The single source of truth for country codes across the system.
 * ISO 3166-1 alpha-2, uppercase. Every field that stores a country
 * (Customer.country, Parcel.originCountry/destCountry, Box.originCountry/
 * destCountry, PricingRule.originCountry/destCountry) MUST use one of these
 * codes — never a free-text country name.
 *
 * Kept intentionally small and curated to the corridors GSG actually
 * operates (USA ↔ West Africa). Extend when a new route opens.
 */
export declare const COUNTRIES: readonly ["US", "BF", "CI", "ML", "SN", "TG", "BJ", "GH", "NE", "GN", "FR"];
export type CountryCode = (typeof COUNTRIES)[number];
export declare const COUNTRY_LABELS: Record<CountryCode, string>;
